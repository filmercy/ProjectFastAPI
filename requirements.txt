# Core Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0

# Database
sqlalchemy[asyncio]==2.0.36
alembic==1.14.0
aiosqlite==0.20.0  # SQLite async driver for development

# PostgreSQL drivers (uncomment when ready to switch to PostgreSQL)
# asyncpg==0.30.0
# psycopg2-binary==2.9.10

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib==1.7.4
argon2-cffi==23.1.0
python-multipart==0.0.20

# File Uploads
aiofiles==24.1.0
# Pillow==11.0.0  # Commented out - requires C compiler on Windows, add later if needed

# Utilities
python-dotenv==1.0.1
email-validator==2.2.0

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1
